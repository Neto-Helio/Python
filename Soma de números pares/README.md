📘 Somador de Números Pares em Python

Este programa pede ao usuário uma quantidade de números, armazena todos em uma lista e depois soma somente os números pares.
Um ótimo exemplo para quem está aprendendo listas, loops e condicionais em Python.

🧠 Como funciona?

O usuário diz quantos números deseja informar.

O programa pede cada número e os guarda em uma lista.

Apenas os números pares são somados.

O resultado final é exibido na tela.

▶️ Como executar

1. Certifique-se de ter o Python 3 instalado.

2. Salve o código em um arquivo, por exemplo: soma_pares.py

3. No terminal, execute:

```bash 
python soma_pares.py